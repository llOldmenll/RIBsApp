<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/primary">

    <TextView
        android:id="@+id/vValue"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/view_margin_medium"
        android:background="@drawable/line_rectangle"
        android:padding="@dimen/view_padding_medium_plus"
        android:textColor="@color/white"
        android:textSize="@dimen/text_size_large_plus"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="London (STL)" />

    <TextView
        android:id="@+id/vTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/view_margin_min"
        android:layout_marginBottom="56dp"
        android:background="@color/primary"
        android:fontFamily="sans-serif-medium"
        android:paddingStart="@dimen/text_padding_min"
        android:paddingEnd="@dimen/text_padding_min"
        android:text="@string/origin"
        android:textColor="@color/accent"
        android:textSize="@dimen/text_size_big"
        app:layout_constraintBottom_toBottomOf="@+id/vValue"
        app:layout_constraintStart_toStartOf="@+id/vValue" />

</androidx.constraintlayout.widget.ConstraintLayout>